// Public config only — NO admin key here.
// After Cloudflare deploy, set endpoint to your workers.dev URL (no trailing slash).
window.BB_ANALYTICS = {
  enabled: true,
  endpoint: '', // e.g. 'https://bb-queue-analytics.<subdomain>.workers.dev'
};
